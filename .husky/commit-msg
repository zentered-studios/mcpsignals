# Entire CLI: strip trailer if no user content (allows aborting empty commits)
if command -v entire >/dev/null 2>&1; then entire hooks git commit-msg "$1" || true; else :; fi
npx commitlint --edit "$1"
