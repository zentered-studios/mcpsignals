/review high 83

---

<task-notification>
<task-id>a27ecee3f2c268208</task-id>
<output-file>/private/tmp/claude-501/-Users-patrick-Sites-mcpsignals/54df9a88-82fd-42bc-8dec-941c29eb97ee/tasks/a27ecee3f2c268208.output</output-file>
<status>completed</status>
<summary>Agent "/code-review high 83" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>```json
[
  {
    "file": "schema/events.md",
    "line": 92,
    "summary": "The documented false-negative mode is wrong for the most common denial wording: the `validation` regex matches `required`, so an undeclared 'Payment required', 'Login required' or 'Authentication required' message buckets as `validation`, not `internal`.",
    "failure_scenario": "A server returns isError with 'Payment required to use this tool.' and no `_meta` kind. classifyError matches `/required/` and records `validation`. Validation-error dashboards fill with billing denials, and readers of events.md expect those rows under `internal`."
  },
  {
    "file": "packages/python/src/mcpsignals/instrument.py",
    "line": 83,
    "summary": "For a Mapping result, `_declared_error_kind` reads only the `_meta` key, but `_is_error_result` also accepts the snake_case `is_error` shape, where the field is named `meta`.",
    "failure_scenario": "call_next returns a dict from `model_dump()` without `by_alias=True` (keys `is_error` and `meta`). `_is_error_result` reports the failure, but `result.get('_meta')` is None. The declared `auth_required` is dropped and the heuristic records `internal`."
  },
  {
    "file": "packages/node/src/instrument.ts",
    "line": 314,
    "summary": "The `_meta` lookup runs inside the whole-event `guardedAsync`, not its own `guarded()` like the response byte count. A throw while reading the declared kind drops the entire failed-call event. The Python `_declared_error_kind` call at instrument.py:264 has the same exposure.",
    "failure_scenario": "A handler returns an isError result whose `_meta` is a Proxy or has a throwing getter (Python: a Mapping whose `.get` raises, or a value whose `==` raises inside `in ERROR_KINDS`). The 'event recording' guard catches it and no tool_call row is written, so the failure disappears from telemetry."
  },
  {
    "file": "packages/node/src/events.ts",
    "line": 4,
    "summary": "`ERROR_KINDS` is exported as a plain mutable array. `as const` is type-only, so any consumer mutation changes what `isErrorKind` accepts. The Python export is an immutable tuple.",
    "failure_scenario": "A consumer runs `(ERROR_KINDS as string[]).push('teapot')` or sorts it in place for a dropdown. `isErrorKind('teapot')` becomes true and arbitrary kinds reach every sink. `Object.freeze([...] as const)` prevents this."
  },
  {
    "file": "packages/python/src/mcpsignals/events.py",
    "line": 23,
    "summary": "events.md now tells EventBuffer users to 'set `error_kind` on the event', but Python `ToolCallEvent.error_kind` is still `str | None`, with no Literal type and no validation. Node gets a compile-time check.",
    "failure_scenario": "A Python host pushes `ToolCallEvent(error_kind='auth-required', ...)` straight to EventBuffer. The typo is written to Postgres/BigQuery unchecked, so dashboards grouping on the six documented values miss those rows. The same code in TypeScript fails typecheck."
  },
  {
    "file": "packages/node/src/index.ts",
    "line": 20,
    "summary": "`isErrorKind` is not exported, and Python has no equivalent. Consumers on the documented direct-EventBuffer path must re-implement the membership check. Python repeats it inline as `kind in ERROR_KINDS`.",
    "failure_scenario": "Each host writing its own events duplicates `ERROR_KINDS.includes(x)`. Adding a seventh kind later means every copy must be found. Exporting `isErrorKind` (and adding `is_error_kind` in error_kind.py) gives one shared check."
  },
  {
    "file": "packages/node/test/buffer.test.ts",
    "line": 45,
    "summary": "The new EventBuffer test cannot fail at runtime. EventBuffer never reads or changes `error_kind`, so the test passes on main as well. Only the type widening is new, and typecheck already covers that.",
    "failure_scenario": "The test suite reports coverage for 'declared kinds via EventBuffer', but reverting `ERROR_KINDS` or the widened `ErrorKind` union leaves this test green at runtime. It adds maintenance cost and no regression protection."
  },
  {
    "file": "packages/node/test/instrument.test.ts",
    "line": 122,
    "summary": "The five new Node tests and five new Python tests wait a fixed 10 ms / 0.05 s for async recording instead of flushing through the handle. That adds ten more timing-dependent tests.",
    "failure_scenario": "On a loaded CI runner, recording takes longer than 10 ms. `events[0]` is undefined and the test fails with a TypeError unrelated to error_kind. Awaiting `handle.flush()` (Node) or `handle_for(server).flush()` (Python) removes the race."
  },
  {
    "file": "packages/node/src/events.ts",
    "line": 19,
    "summary": "The telemetry-only `_meta['mcpsignals/error_kind']` key goes to the client unchanged. Its prefix is a bare label, not the reverse-DNS form the MCP `_meta` naming guidance recommends.",
    "failure_scenario": "Every denied call leaks an internal telemetry label to third-party MCP clients, and a client that surfaces `_meta` shows it to users. A host that later renames or drops the library leaves clients depending on the key. Stripping the key after reading it, or using a reverse-DNS prefix such as `com.zentered.mcpsignals/error_kind`, avoids both."
  },
  {
    "file": "packages/python/src/mcpsignals/error_kind.py",
    "line": 8,
    "summary": "The two packages define `ERROR_KINDS` and `ERROR_KIND_META_KEY` in different modules: Node puts them in events.ts, Python in error_kind.py. Two test files hardcode the list to keep them in sync.",
    "failure_scenario": "A maintainer adding a kind updates events.ts and the Node test but not error_kind.py, because the modules do not mirror each other. The packages then accept different declared kinds and record different values for the same `_meta`."
  }
]
```

This reviews PR #83 (`feat: add declared error_kind for sign-in and payment denials`). Nothing here was checked with a verifier pass and none of it was run. The first finding matters most: the heuristic's `required` pattern files undeclared "Payment required" or "Login required" messages as `validation`, not `internal` as schema/events.md says. No repo CLAUDE.md exists, and the diff breaks no rule in ~/.claude/CLAUDE.md.

A `git checkout` of the PR branch was refused because of your uncommitted changes, and your working tree is unchanged. The review read the branch through `git show` and `git grep` instead.</result>
<usage><subagent_tokens>89794</subagent_tokens><tool_uses>8</tool_uses><duration_ms>101653</duration_ms></usage>
</task-notification>

---

fix all issues, simplify, merge