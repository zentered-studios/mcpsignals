[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcpsignals"
# Placeholder, and deliberately not kept in sync by hand. semantic-release
# stamps the real version in at release time and never commits it back, exactly
# as packages/node/package.json is left at 0.1.0. Do not read this as the
# published version - check PyPI or the git tags for that.
version = "0.1.0"
description = "Drop-in instrumentation for MCP servers. Writes tool-call events to a database you own."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "mcpsignals contributors" }]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = ["mcp>=2.0.0"]

[project.optional-dependencies]
postgres = ["asyncpg>=0.29"]
bigquery = ["google-cloud-bigquery>=3.11"]
otlp = ["opentelemetry-api>=1.20"]

[project.urls]
Homepage = "https://github.com/zentered-studios/mcpsignals#readme"
Repository = "https://github.com/zentered-studios/mcpsignals"
Issues = "https://github.com/zentered-studios/mcpsignals/issues"

# Not an optional-dependencies extra: dev tooling should not be installable by
# consumers as `pip install mcpsignals[dev]`. PEP 735 groups stay out of the
# published metadata entirely.
[dependency-groups]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24", # required by asyncio_mode = "auto" below
    "ruff>=0.15,<0.16",     # single source of truth for the version CI uses
]

[tool.hatch.build.targets.wheel]
packages = ["src/mcpsignals"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
